<script>
  function sendEmail() {
    const recipient = "gonzodim@gmail.com";
    const subject = document.getElementById("subject").value;
    const name = document.getElementById("name").value;
    const phone = document.getElementById("phone").value;
    const description = document.getElementById("description").value;

    const mailtoLink = `mailto:${recipient}?subject=${encodeURIComponent(
      subject
    )}&body=Consulta de: ${name}%Numero de Telefono: ${phone}%Descripcion: ${encodeURIComponent(
      description
    )}`;
    window.location.href = mailtoLink;
  }

  const button = document.querySelector(".button-mail");
  button.addEventListener("click", sendEmail);
</script>

<form>
  <h2>Envianos un mail y dejanos tu consulta</h2>
  <label for="subject">Tipo de Evento:</label>
  <select id="subject" name="recipient">
    <option value="gonzodim@gmail.com">Salón Exclusivo</option>
    <option value="sampa@gmail.com">Recipient 2</option>
    <option value="gonzodim@gmail.com">Recipient 3</option>
    <option value="gonzodim@gmail.com">Recipient 4</option>
  </select>
  <br />

  <label for="name">Nombre:</label>
  <input type="text" id="name" name="name" maxlength="20" required />
  <br />
  <label for="phone">Telefono:</label>
  <input type="text" id="phone" name="phone" maxlength="20" required />
  <br />

  <label for="description">Dejanos tu consulta aca:</label><br />
  <textarea id="description" name="description" rows="4" cols="50" required
  ></textarea>
  <br />

  <button class="button-mail" type="button" onclick="">Send Email</button>
</form>
<style>
  body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    background-color: #f1f1f1;
  }

  .recipent {
  }

  .card {
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    background-color: #fff;
    width: 400px;
  }

  form {
    margin: 0;
  }

  form label,
  form select,
  form input,
  form textarea {
    display: block;
    margin-bottom: 10px;
  }

  .button-mail {
    margin-top: 10px;
    background-color: #4caf50;
    color: white;
    padding: 10px 15px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }

  .button-mail:hover {
    background-color: #45a049;
  }
</style>
